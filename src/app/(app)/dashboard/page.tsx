import { createClient } from "@/lib/supabase/server";
import { signOutAction } from "@/lib/actions";
import { Bell, BookOpen, ChevronDown, CircleDollarSign, GraduationCap, LayoutDashboard, LogOut, Mail, Search, Settings, Users } from "lucide-react";

const menu = [["Dashboards", LayoutDashboard],["Enrollment", Users],["Course", BookOpen],["Manage Teacher", GraduationCap],["Payment Gateway", CircleDollarSign],["Message", Mail],["Notification", Bell],["Setting", Settings]] as const;

export default async function Dashboard() {
  const supabase = await createClient();
  const { data: u } = await supabase.auth.getUser();
  if (!u.user) return <main className="p-6">Sessão expirada</main>;
  const { data: profile } = await supabase.from("users_profiles").select("organization_id,full_name").eq("id", u.user.id).single();
  const orgId = profile?.organization_id;

  const [courses, enrollments, certs, announcements] = await Promise.all([
    supabase.from("courses").select("id", { count: "exact", head: true }).eq("organization_id", orgId),
    supabase.from("enrollments").select("id", { count: "exact", head: true }).eq("organization_id", orgId),
    supabase.from("certificates").select("id", { count: "exact", head: true }).eq("organization_id", orgId),
    supabase.from("announcements").select("id", { count: "exact", head: true }).eq("organization_id", orgId),
  ]);

  const kpis = [["Total Courses", courses.count ?? 0, "+12.05%"],["Total Enrolments", enrollments.count ?? 0, "+12.05%"],["Last Year Earning", "$12,122", "-12.25%"],["Total Students", certs.count ?? 0, "+12.05%"],["Total Instructor", announcements.count ?? 0, "+12.05%"]];

  return <main className="min-h-screen p-2 md:p-6"><section className="mx-auto max-w-[1440px] rounded-[36px] border border-black/10 bg-white/95 p-2 md:p-4"><div className="grid gap-4 md:grid-cols-[230px_1fr]"><aside className="card hidden h-fit p-3 md:block"><div className="mb-5 flex items-center gap-2 px-2"><div className="rounded-lg bg-violet-100 p-2 text-violet-600"><GraduationCap size={16} /></div><span className="text-sm font-bold tracking-wide">LMS</span></div><nav className="space-y-1.5">{menu.map(([label,Icon],i)=><button key={label} className={`flex w-full items-center justify-between rounded-lg px-3 py-2 text-[12px] ${i===0?"bg-violet-50 text-violet-700":"text-slate-600 hover:bg-slate-50"}`}><span className="flex items-center gap-2"><Icon size={14}/>{label}</span><ChevronDown size={12} className="opacity-50"/></button>)}</nav><form action={signOutAction} className="mt-5"><button className="flex w-full items-center justify-center gap-2 rounded-lg border border-slate-200 px-3 py-2 text-xs text-slate-600"><LogOut size={14}/>Logout</button></form></aside><div className="space-y-4"><header className="card flex flex-wrap items-center justify-between gap-3 p-4 md:p-5"><div><h1 className="text-xl font-bold md:text-[28px]">LMS Admin Dashboard</h1><p className="text-[12px] text-slate-500">{profile?.full_name}</p></div><div className="flex items-center gap-2 text-xs"><button className="soft px-3 py-1.5">USD</button><button className="soft px-3 py-1.5">Eng</button><button className="soft p-2"><Bell size={14}/></button><button className="soft p-2"><Search size={14}/></button></div></header><section className="grid gap-3 sm:grid-cols-2 xl:grid-cols-5">{kpis.map(([t,v,g])=><article key={String(t)} className="card p-4"><p className="text-[11px] text-slate-500">{t}</p><p className="mt-2 text-[34px] font-bold leading-none">{String(v)}</p><p className={`mt-2 text-[11px] ${String(g).startsWith('-')?'text-rose-500':'text-emerald-500'}`}>{String(g)}</p></article>)}</section><section className="grid gap-3 xl:grid-cols-[1.6fr_1fr]"><article className="card p-4"><div className="mb-3 flex items-center justify-between"><h3 className="text-sm font-semibold">Overview</h3><span className="soft px-2.5 py-1 text-[11px]">Last 7 Days</span></div><div className="grid h-52 grid-cols-7 items-end gap-2">{[60,95,58,120,76,100,84].map((h,i)=><div key={i} className="rounded-t-md bg-violet-500/80" style={{height:`${h}px`}}/>)}</div></article><article className="card p-4"><h3 className="mb-3 text-sm font-semibold">Revenue Statics</h3><div className="grid h-52 grid-cols-12 items-end gap-1">{[35,44,28,60,40,36,52,57,49,62,55,58].map((h,i)=><div key={i} className="rounded-t bg-indigo-300/80" style={{height:`${h}px`}}/>)}</div></article></section><section className="grid gap-3 xl:grid-cols-[1.6fr_1fr]"><article className="card overflow-auto p-4"><h3 className="mb-3 text-sm font-semibold">Best Selling Courses</h3><table className="w-full min-w-[700px] text-xs"><thead><tr className="text-left text-slate-500"><th className="py-2">Course Name</th><th>Instructor</th><th>Sales</th><th>Amount</th></tr></thead><tbody>{["Starting Online Courses & Build your Skills","Deploy Career in a Short Online Courses","Complete Java Program Online Courses","Training and Learning Online"].map((c,i)=><tr key={c} className="border-t border-slate-100"><td className="py-2.5">{c}</td><td>{["Jane Doe","Mary Johnson","Linda Davis","Patricia Taylor"][i]}</td><td>{[200,300,190,350][i]}</td><td>${[900,1500,650,1860][i]}</td></tr>)}</tbody></table></article><article className="card p-4"><h3 className="mb-3 text-sm font-semibold">Top Selling Instructor</h3><div className="mx-auto my-4 h-40 w-40 rounded-full border-[18px] border-violet-200 border-t-violet-500 border-r-emerald-400 border-b-orange-400"/><ul className="space-y-1.5 text-xs">{["Jane Doe","Linda Davis","Susan Hall","Thomas","Elizabeth","Robert"].map((n,i)=><li key={n} className="flex items-center justify-between"><span className="text-slate-600">{n}</span><span className="text-violet-600">{[69,88,65,50,48,32][i]}%</span></li>)}</ul></article></section></div></div></section></main>;
}
